import { EditIssueByEmployee } from './edit-issue-by-employee';

describe('EditIssueByEmployee', () => {
  it('should create an instance', () => {
    expect(new EditIssueByEmployee()).toBeTruthy();
  });
});
