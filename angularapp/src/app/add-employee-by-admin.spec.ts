import { AddEmployeeByAdmin } from './add-employee-by-admin';

describe('AddEmployeeByAdmin', () => {
  it('should create an instance', () => {
    expect(new AddEmployeeByAdmin()).toBeTruthy();
  });
});
